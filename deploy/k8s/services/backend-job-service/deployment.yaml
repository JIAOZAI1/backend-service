apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-job-service
  namespace: default
  labels:
    app: backend-job-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-job-service
  template:
    metadata:
      labels:
        app: backend-job-service
    spec:
      containers:
        - name: backend-job-service
          image: ghcr.io/jiaozai1/backend-job-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "dev"
            # MySQL / RabbitMQ 统一从 config-dev-secret 注入（见 deploy/k8s/base/secret-example.yaml）。
            # MYSQL_* 仅用于拼接下方 ConnectionStrings__MySql，需定义在其之前。
            - name: MYSQL_HOST
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: mysql-host
            - name: MYSQL_PORT
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: mysql-port
            - name: MYSQL_DATABASE
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: mysql-database
            - name: MYSQL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: mysql-username
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: mysql-password
            - name: ConnectionStrings__MySql
              value: "Server=$(MYSQL_HOST);Port=$(MYSQL_PORT);Database=$(MYSQL_DATABASE);User=$(MYSQL_USERNAME);Password=$(MYSQL_PASSWORD);"
            - name: RabbitMq__Host
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: rabbitmq-host
            - name: RabbitMq__Port
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: rabbitmq-port
            - name: RabbitMq__VirtualHost
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: rabbitmq-vhost
            - name: RabbitMq__Username
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: rabbitmq-username
            - name: RabbitMq__Password
              valueFrom:
                secretKeyRef:
                  name: config-dev-secret
                  key: rabbitmq-password
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi

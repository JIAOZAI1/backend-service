apiVersion: v1
kind: Service
metadata:
  name: backend-job-service
  namespace: default
  labels:
    app: backend-job-service
spec:
  selector:
    app: backend-job-service
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP

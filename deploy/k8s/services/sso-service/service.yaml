apiVersion: v1
kind: Service
metadata:
  name: sso-service
  namespace: default
  labels:
    app: sso-service
spec:
  selector:
    app: sso-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP

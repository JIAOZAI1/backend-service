apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: default
  labels:
    app: admin-service
spec:
  selector:
    app: admin-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
